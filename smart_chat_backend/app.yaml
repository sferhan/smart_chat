service: backend
runtime: python311
env: standard
entrypoint: daphne -p $PORT -b 0.0.0.0 smart_chat_backend.asgi:application


env_variables:
  APPENGINE_URL: backend-dot-smart-chat-383601.wl.r.appspot.com

handlers:
- url: /.*
  script: auto

runtime_config:
  python_version: 3
